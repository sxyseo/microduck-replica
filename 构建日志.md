# 构建日志

**简体中文** · [English](BUILD-LOG.en.md)

> **当前状态：3D 打印中**
> 最后更新：2026-09-02　|　已有实物照片 ↓

本仓库的其余部分都是**纸上分析** —— 从公开的 MJCF 与源码反推出的几何、装配关系和电控方案。
这份日志记录的是**把它真正做出来**的过程：打了什么、装了什么、哪里出了问题。

> ⚠️ 本仓库反复强调过一个判断：**仿真 STL 不是可打印工程件** ——
> 仿真只保证外形与惯量，不保证配合公差、螺纹、埋件座和走线空间。
> **这份日志就是在验证这句话到底对不对。** 结论会如实写在这里，无论正反。

---

## 进度

| 日期 | 阶段 | 状态 | 备注 |
|---|---|---|---|
| 2026-09-01 | 首批零件打印 | 🔨 进行中 | 头壳、躯干壳、腿部结构件、脚 |
| 2026-09-02 | 腿部件试装 | 🔨 进行中 | **M2 螺丝已装入，孔位可用** |
| 2026-09-06 | 代打询价 + 定标件备齐 | 🔨 进行中 | ¥99 店（PLA 41 件）+ 嘉立创 ¥266 询价；测试片 STL 就绪，先打测试片 |
| 2026-09-06 | `imu_to_dxl` 连接表 | ✅ 完成 | 网络/引脚预算表就绪，可照表在立创 EDA 画图 |
| 2026-09-06 | exp001 A/B 训练 | ✅ 完成 | 本机 CPU 8 分钟跑完，B 组末段领先（1.014 vs 0.801），详见踩坑记录 |

图例：📋 计划中 · 🔨 进行中 · ✅ 完成 · ⚠️ 遇到问题 · ❌ 此路不通

---

## 打印记录

| 零件 | 材料 | 层高 | 填充 | 耗时 | 结果 |
|---|---|---|---|---|---|
| *(待填)* | | | | | |

**打印机 / 切片软件**：*(待填)*

---

## 踩过的坑

> 这一节是整份日志最有价值的部分 —— **别人可以不重复踩。**
> 每条按「现象 → 原因 → 解决」写，没解决的就写「未解决」。

### 2026-09-06 · RL 训练在本机（无 GPU 的 Mac）跑不起来

**现象**：`uv run train` 直接崩 `IndexError: list index out of range`（mjlab/utils/gpu.py 的 `select_gpus`）。
**原因**：训练器默认 `gpu_ids=[0]`，无 CUDA 的机器上 GPU 列表为空，按索引取值越界。
**解决**：环境变量 `CUDA_VISIBLE_DEVICES=""`（mjlab 官方约定的显式 CPU 模式，gpu.py 文档字符串写明）。
同一天第二个坑：默认 logger 是 wandb，没配 API key 直接报错——加 `--agent.logger tensorboard`（与官方冒烟参数一致）。

### 2026-09-06 · exp001 A/B 实验完成（CPU 上 8 分钟跑完预注册方案）

**过程**：原以为 100 迭代 × 64 环境要在本机跑一晚，实测**单组仅 4 分钟**（约 2.3s/迭代，
之前 lesson-01 的"10 分钟"几乎全是启动开销）。A（IMU 随机化 ON）与 B（OFF，一行补丁）先后跑完，
补丁已还原，上游仓库保持干净。
**结果**：B（世界更乖）末段 reward 反超（1.014 vs 0.801），其余四项指标几乎重合——
预注册的预案命中："100 迭代只能得出前期动力学差异"。随机化的鲁棒性收益要用扰动测试看（exp001b 待办）。
**记录**：`rl-series/experiments/exp001-imu-randomization/`（记录-A/记录-B + 对比图）。

### 2026-09-06 · 代打 TPU 被拒：软下巴/软嘴用 PLA 顶的代价评估

**现象**：¥99 按重代打店只做 PLA，TPU 两件（软下巴 18.6cm³ + 软嘴 2.7cm³，共 26g）被拒。
**评估**：影响 = 叼取技能退化（鸭嘴实为软夹爪）+ 连杆公差无弹性易卡；**不影响走路/装配/发声**
（嘴不在策略动作空间）。**解决**：两件单独找 TPU 渠道补打（嘉立创起步 22.5 元，约 ¥30–50），
不在关键路径（第 3 周才装头部）；PLA 版可先记为已知偏差。勿让店家降填充冒充软料。

### 2026-09-06 · `imu_to_dxl` 原理图连接表完成（引脚编号策略）

**问题**：固件/规格都齐了，画原理图卡在"引脚编号不敢凭记忆写"。
**解决**：采用**网络名 + 引脚名**的连接表（`docs/imu_to_dxl-设计包/原理图连接表.md`），
引脚编号交给立创 EDA 的商城符号（C529333 等）自动对应——这正是嘉立创工作流的正确姿势。
表里埋了两个新手最易翻车的点：LSM6DSV16X 的 **CS 必须拉 3V3**（悬空进 SPI 模式，I2C 读不到）、
**LDO 输入电容耐压 ≥25V**（挂 8.4V 总线，10V 件长期超压）。

### 2026-09-06 · exp001b 扰动测试完成 + 又踩了一次路径坑

**过程**：给评估脚本写输出路径时用了 `../rl-series/...` 相对路径，从 upstream 目录运行就找错位置——
**本会话第二次栽在相对路径上**。教训已固化：**脚本内一律基于 `__file__` 定位仓库相对路径**。
**结果**：A（随机化）在全角度区间更平稳（最大衰减 -4.6% vs B 的 -6.0%，10° 起 A 反超），
但 1–2% 的差异在单 seed 噪声内——100 迭代尺度下的诚实结论是"不可测"，工具已固化为
`rl-series/scripts/eval_imu_robustness.py`，长训练 checkpoint 出来即可复测。
**意外收获**：评估打印的网络结构直接显示了 critic `in_features=76`，为 76 维特权观测再添运行时证据。

### 2026-09-06 · 3D 仿真回放环境搭通（离屏渲染 + 交互查看两条路）

**过程**：用 mjlab 的 `render_mode="rgb_array"`（内部 OffscreenRenderer）写了
`rl-series/scripts/capture_rollout.py`：加载 checkpoint → 单环境滚动 240 控制步 → 每 2 步渲染一帧 →
合成 GIF + 关键帧 PNG。相机用 `ViewerConfig.origin_type=ASSET_ROOT` 跟拍鸭子本体，
四分之三视角（与静态渲染出图角度一致）。
**结果**：A/B 两组 100 迭代 checkpoint 各出一段回放（`rl-series/assets/回放-*.gif`）。
单次回放 A 摔 1 次、B 摔 4 次——方向与 exp001b 一致但属噪声，别过度解读。
**踩坑与释疑**：首次跑完看"帧亮度均值 58/255"以为渲染黑屏，看关键帧才发现是**上半屏黑天空**
拉低了均值，鸭子本体光照完全正常——教训：**统计指标会骗人，画面本身才是事实**。
**交互查看**：`uv run play ... --viewer native`（弹出 3D 窗口可鼠标旋转）/ `--viewer viser`（浏览器），
命令已写进 `rl-series/README.md`。100 迭代的鸭子还在踉跄——这正是"第一次回放"的诚实素材。

### 2026-09-07 · 舵机采购落地：飞特 HL-1910-C001 ×15（¥1500）——换舵机路线正式激活

**决策**：主控缺货期以 ¥100/只购入飞特 HLS 恒力系列 TTL 总线舵机（磁编码、电流环），
替代 XL330 方案（¥2580+），**省约 ¥1100，整机预计 ≈¥2460 低于官方售价**。
**代价（四层影响，详见物料快照"一⁺"节）**：①几何——打印件舵机仓按 XL330 设计，
HL 尺寸/孔位适配 = 头号验证项（转接板或局部重画）；②电气——HL 7.4V 级与 2S 匹配更规范；
③通信——飞特协议非 Dynamixel V2：FE-URT-1（飞特自家板）对口验证，`imu_to_dxl` 固件走
预留的飞特分支（硬件不变），Rust 运行时 bus.rs 需适配层；④动力学——**官方 9 个 ONNX 作废，
必须重训**（expC checkpoint 转任 L2 退化基线；重训流程按《换舵机重训全流程》）。
**物料快照已更新**（`docs/物料清单-进度快照.md`）：已花 ¥1898，剩 ≈¥560 分三批。

### 2026-09-08 · 采购单改版：价格快照 → 规格导向（用户反馈"价格不对、规格没写"）

**问题**：此前采购清单以"链接 + 快照价"为主，电商价格漂移导致对不上，且部分条目没有写
验收规格，用户"不知道买哪个"。
**修正**：`docs/物料清单-进度快照.md` 第二节整体改版为**规格导向采购单**——每项 =
搜索词 + 验收规格（可任何店铺比对）+ 数量 + 约价区间；链接降级为参考。
**新增关键规格**：连接线长度按鸭子几何定量（10/15/20cm 各 5 条，30cm+ 不要），且**等
HL-1910 到货确认出线插头（5264 还是 XH/PH）再下单**；轴承写尺寸与防尘盖规格（防买成
推力轴承/法兰）；电池写外形尺寸排除 F970；扣板写"输出须 7.4V 直出"（防买成只出 5V 的）；
转接板写"确认 1Mbps"；排线写 pin 距与长度上限。教训：**采购单的生命是规格，不是链接。**

舵机（~~待核 XL330-M288-T~~ → **实购飞特 HL-1910-C001 ×15 ¥1500，见上条**）、Radxa Zero 3W（✅缺货解决）、外壳 41 件（✅到手待实测）。
新增 `docs/物料清单-进度快照.md`：剩余清单分三批——①本周 ¥180（调试板/线材/轴承/螺丝/
热熔螺母/乐泰/TPU 件）②电子 ¥280（电池+扣板/转接板+UBEC/摄像头/ToF）③自制板 ¥100
（imu_to_dxl 打样+元件，连接表已备）。整机到手封顶 ≈¥3500–3900，落在预估区间。
到手后三件动手事：G1 收货实测 → G2 单舵机验总线（写 return_delay=0）→ Zero 3W 刷机过三坑。

### 2026-09-06 · 交互查看实测：macOS + uv 环境上 native 窗口是死路，viser 是正解

**现象**（用户实跑报告）：`uv run play --viewer native` 在全部环境加载成功后崩在
`RuntimeError: launch_passive requires that the Python script be run under mjpython on macOS`。
**排查链**：① 直跑 `.venv/bin/mjpython .venv/bin/play ...` → dlopen 失败
（uv 的独立版 Python 是符号链接，`@rpath/libpython3.12.dylib` 在 mjpython 的嵌入环境里解析不到）；
② `-m` 模块方式同样死；③ `DYLD_LIBRARY_PATH` 指向 uv Python 的 lib 目录无效（搜索列表不含它）。
**结论**：mujoco 的 mjpython 与 uv 管理的 venv 在 macOS 上不兼容，属环境级问题，不值得继续 hack。
**解决**：`--viewer viser`（浏览器页面，不经 launch_passive）实测可用——启动后浏览器开
`http://localhost:8080` 即可实时看鸭子。过程中为验证会短暂弹出/关闭过几个窗口。
（顺带：用户日志里的 ObservationManager 表直接印证了 actor 61 / critic 76 维和文章 07 的 reward 表——
官方诊断输出本身就是最好的教学材料。）

### 2026-09-06 · "localhost:8080 打不开"——根因是服务器没在运行

**现象**：用户浏览器开 `http://localhost:8080/` 打不开。
**根因**：不是网络问题，是**服务端根本没在监听**（`lsof -i :8080` 为空）——我之前验证 viser 时
跑完即清了进程，用户的浏览器自然扑空。教训：**给"打不开网页"做诊断，第一步永远是查端口有没有
监听，而不是猜浏览器/代理的问题。**
**解决**：`nohup + disown` 拉起常驻 viser 服务器，`curl` 验证 HTTP 200（页面 2.9MB）后交付。
**优化**：新增 `rl-series/scripts/serve_play.sh` 一键启动脚本——自动用默认 checkpoint、
已在运行则不重复拉起、最多等 60 秒做就绪自检，失败提示看日志。

### 2026-09-06 · expC 续训启动（回答"3 万步为什么不会走"）

**背景**：用户在回放里看到 3 万步不会走。诊断：回放是冻结权重的重放，不产生学习；
真正差距是训练量——exp001 的 15 万步 vs 官方配方的 49 亿步（0.003%）。
**决策**：按路径 A 做实证——CPU 续训 2000 迭代，检验"量变能否引发布态"。
**踩坑**：tyro 的布尔参数必须写 `--agent.resume True`，只写 flag 报 invalid choice；
先 5 迭代冒烟确认 `Loading model checkpoint from: ...model_99.pt` 且迭代号从 99 续起，再放全量。
**启动**：run_name `expC-cpu2000`，nohup 常驻，约 80–110 分钟；checkpoint 每 250 迭代自动存。
预注册假设与判定标准见 `rl-series/experiments/expC-cpu2000/README.md`。

### 2026-09-06 · 可视化操作台上线 + 《换舵机重训全流程》手册 + expC 中期战报

**① 可视化控制台**：`rl-series/scripts/dashboard.py`（纯标准库，http://localhost:8090）——
状态面板（训练进度/奖励自动刷新）+ 按钮操作（启动/停止续训、TensorBoard、viser、
按 checkpoint 捕获回放 GIF）+ 产物画廊。端到端实测：点击捕获按钮 → 2 分钟后
`回放-面板-exp001-a-model_99.gif` 出现在画廊。**GET 永不阻塞、长任务后台派生**是设计红线。
**② 手册**：`docs/换舵机重训全流程.md`——四层影响面判定表、单舵机台架辨识（买 15 只之前
先测 1 只，产出 BAM 参数）、9 策略↔任务映射与训练优先级、树莓派两用法（调试台/机载移植）、
L1–L7 测试金字塔。
**③ expC 中期战报（迭代 336/2199）**：mean reward **2.10**——**预注册 H1（破 2.0）提前达成**；
fell_over 降到 **1.54–1.67**（从 ~2.0 明显下行，H2 进展中）；episode length 51 步。
量变→质变的曲线正在兑现，等 2000 迭代后用 model_2000 拍"第一次迈步"回放。

### 2026-09-06 · expC 终判：H1/H2/H3 全部达成 + 现有器材启动指南

**expC 跑完（2199 迭代，85 分钟）**：reward 0.80 → **12.77**（16 倍），fell_over → **1.00**，
episode length 51 → **315 步**；最终 checkpoint 回放 **240 步零摔倒**，关键帧见腿部换姿态（碎步非大步）。
**H1✅（大幅超越）H2✅ H3✅（基本）**。"3 万步不会走"问题实证收口：**不是模型不能走，是训练量不到**
——同配方同机器，续训 2100 迭代即质变。四档中间 checkpoint 保留可回放步态涌现过程；
也是将来"换执行器 → L2 退化基线"的现成测试对象。
**器材盘点**（¥99 打印件到货 + SG90×6 + Pi 2 + ESP32 配件）：SG90 不能上鸭（PWM 无反馈/力矩 1/3/孔位不同）
但 + Pi 2 是最佳 PWM 入门台架（四步实验脚本 `rl-series/scripts/pi_sg90_bench.py`，第④步"开环漂移"
正是"为什么必须总线舵机"的实物课）；Pi 2 的 PL011 UART 做总线调试反而省事；ESP32/扩展板封存备用。
G2 门票不变：**FE-URT-1 + 1 只总线舵机 ≈¥220**。详见 `docs/手头器材启动指南.md`。

---

## 尺寸与配合验证

反推出来的数据在实物上对不对：

| 项目 | 反推值 | 实测值 | 是否吻合 |
|---|---|---|---|
| 整机外形 | 144 × 141 × 264 mm | *(待测)* | |
| M2 过孔 | Ø2.2 mm | *(待测)* | |
| M2 沉头孔 | Ø4.4 mm | *(待测)* | |
| M2 攻丝底孔 | Ø1.6 mm | *(待测)* | |
| 轴承座 | Ø22 × 16 × 4 mm | *(待测)* | |

> FDM 打印通常会让孔径偏小 0.1–0.3 mm，这一栏的实测数据对后来者价值很大。

---

## 待解决

- [ ] *(待填)*

---

## 照片

### 2026-09-02 · 首批打印件

![首批打印件](build-log/photos/2026-09-02-首批打印件.jpg)

已打印并部分装配的零件：

- **头部总成** —— 多色打印（白 / 黑 / 红），含摄像头环座与指示灯开口，表面质量良好
- **颈部** —— 黑色，已与头部连接
- **躯干外壳** —— 白色
- **腿部结构件** —— 黑色，**M2 螺丝已装入**
- **脚** —— 红 / 黄双色，两只

> 📌 **第一个实物验证点**：黑色腿部件上的 **M2 螺丝已经装进去了**。
> 这说明 [紧固件反推](docs/紧固件反推.md) 得出的「整机是 M2 螺丝系统」这个结论，
> 在实物上是成立的 —— 至少这几个孔位是。
>
> 精确孔径实测数据待补，见上方「尺寸与配合验证」表。
