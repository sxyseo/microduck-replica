# exp000 · 首次冒烟：5 迭代 + CPU smoke（已完成）

> 状态标签：**【已验证】**——以下全部数据由脚本从事件文件原样提取（`scripts/extract_tb_metrics.py`），非手抄。

```text
实验名称：exp000 链路冒烟（train → tensorboard → checkpoint → ONNX）
代码 commit：microduck_rl @ 29e887e（2026-09-02, "Merge pull request #30 from peterschade/sim-body-server"）
本地改动：无
修改参数：无（冒烟测试，只缩规模）
我的假设：5 次迭代足够验证链路全通，但不足以让鸭子学会任何东西
训练命令：
  # cpu-smoke（第 0 次试探，8 环境）
  uv run train Mjlab-Velocity-Flat-MicroDuck
  # lesson-01（正式冒烟）
  uv run train Mjlab-Velocity-Flat-MicroDuck --env.scene.num-envs 8 --agent.max_iterations 5
  （注：params/agent.yaml 快照显示 seed=42, num_steps_per_env=24, num_envs=8）
机器与硬件：Mac mini（Apple Silicon），纯 CPU，microduck_rl/.venv（uv sync）
训练时长：三次 lesson-01 运行分布在 00:03–00:23，每次约 10 分钟内
显存占用：-（CPU）
```

## 产物清单（logs/rsl_rl/velocity/）

| 运行 | 产物 |
|---|---|
| `2026-09-04_23-59-23_cpu-smoke` | tfevents ×1 迭代、model_0.pt、cpu-smoke.onnx |
| `2026-09-05_00-23-33_lesson-01`（采用） | tfevents 5 迭代、model_0–4.pt、lesson-01.onnx、params/、git/ |

ONNX 形状（`inspect_onnx.py` 实测）：`INPUT obs [1,61] / OUTPUT actions [1,14]` ✅

## TensorBoard 变化（lesson-01 原值）

| iteration | mean_reward | fell_over | error_vel_xy | error_vel_yaw | mean_std |
|---|---|---|---|---|---|
| 0 | — | 0.000 | 0.000 | 0.000 | 1.001 |
| 1 | -0.061 | 1.375 | 0.024 | 0.137 | 1.000 |
| 2 | 0.007 | 1.000 | 0.032 | 0.135 | 1.000 |
| 3 | 0.090 | 1.000 | 0.035 | 0.153 | 0.999 |
| 4 | 0.095 | 1.000 | 0.028 | 0.172 | 0.999 |

曲线图：`../assets/lesson01-首次5次迭代真实曲线.png`

## 最终结论

- 【已验证】训练、TensorBoard、checkpoint、ONNX 导出四环节全部打通。
- 【已验证】reward 转正但绝对值极小；`fell_over ≈ 1.0`（每集一摔）；`mean_std` 几乎不动（纯探索期）。
- 【已验证】三次 lesson-01 运行指标逐位一致（同 seed 可复现）——随机种子固定后实验是确定性的，这对后面 A/B 对比是好消息。
- 【推测】error_vel_yaw 的上行是早期探索震荡；500+ 迭代仍上行则视为 reward 配方问题。

## 下一步

exp001：IMU 姿态随机化 A/B（`exp001-imu-randomization/`），50–100 迭代量级。
