# 截图清单 · 哪些图要你亲手截

> 原则：**能脚本生成的不截图，必须人眼看到的东西才截图**。截图是证据，不是装饰。
> 已生成的资产见本目录（无需重截）；下面是按 30 天计划的待截清单。

## 已生成（本目录，直接可用）

| 文件 | 来源 |
|---|---|
| `duck-render-01-正面 / 02-侧面 / 03-四分之三.png` | `scripts/render_duck.py` 离屏渲染 |
| `lesson01-首次5次迭代真实曲线.png` | `scripts/plot_ab_compare.py` 读真实事件文件 |
| `obs-61维布局.png` | `scripts/diagram_obs_layout.py` |
| `flags-当前值.txt` | `scripts/show_flags.py` |
| `终端输出-list-envs.txt` | 真实运行存档 |

## D3–D5 · 环境与首次训练

- [ ] `uv run list-envs` 的终端全屏截图（文章 03）
- [ ] TensorBoard 网页界面：`Train/mean_reward` + `Episode_Termination/fell_over` 两图同屏（文章 04 主图，配真实曲线 PNG 互相印证）
- [ ] `logs/rsl_rl/velocity/<run>/` 目录 `ls -la`（展示 model_0–4.pt + onnx + params 同框，文章 04）

## D10–D12 · exp001

- [ ] A/B 两组启动时终端首屏（打印 flags/patch 生效的证据）
- [ ] exp001-AB对比.png 生成后，同屏截 TensorBoard 里两组曲线（脚本性 PNG + 界面截图双证）
- [ ] `git diff` 显示只有 L40 一行差异（诚实性证据，放文章 08）

## D18–D21 · GPU 长训练

- [ ] `nvidia-smi` 训练中截图（显存/利用率，文章 09）
- [ ] HF Jobs 页面或日志（若走 `--hf-jobs`，文章 09）
- [ ] 200+ 迭代后的 TensorBoard 全景（五指标 + episode length，文章 09/11）

## D24–D25 · 运行时与回放

- [ ] `uv run play` 的 MuJoCo viewer 窗口（鸭子走路或摔倒，都截——摔倒也是素材，文章 11）
- [ ] `scripts/inspect_onnx.py` 对新 checkpoint 的输出（61/14，文章 10）

## 公众号运营备注

- 封面图统一用三视图之一 + 大字标题；代码截图务必放大字体（手机上 14px 以下不可读）。
- 所有截图右上角保留时间/机器信息，配合「已验证」标签使用。
