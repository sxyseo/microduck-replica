# 复刻关键路径 · 30 天（2026-09-05 → 10-04）

> 与 `rl-series/30天计划.md`（RL/公众号线）配套：**这条是「鸭子活过来」的硬件线**。
> 两条线每周交汇一次：硬件的每个里程碑都喂给公众号一篇文章，训练线的每个实验都为真机部署铺路。
> 标签：🔨 动手 ｜ 🛒 采购 ｜ 💻 电脑上做 ｜ ✋ 必须人在场

## 关键路径总览（决定整体进度的只有四条依赖链）

```
A. 结构件: 测试片 → 定孔径补偿 → 批量打印 41 件 → 试装
B. 电路:   imu_to_dxl 原理图/PCB → 打样(3-5天) → 贴片烧录 → 单板自测 → 总线联调
C. 采购:   舵机×15(最慢!) → 主控/电池/传感器 → 到货验收
D. 软件:   Radxa 刷 Armbian → 三个必踩的坑 → 官方 Rust 运行时跑通 → 装 ONNX → 遥控
```

**第一天就该下单的**（等货期）：🛒 XL330 舵机 ×15（国际到货 1–3 周，见 BOM.md 核价）、🛒 Radxa Zero 3W、🛒 电池/IMX219/VL53L8CX/LSM6DSV16X/HT7333/SN74LVC1G126/STM32G031F8P6（按 `docs/imu_to_dxl-设计包/BOM.md` + `docs/电控采购清单.md`）。

---

## 第 1 周（D1–7）：测试片定标 + imu_to_dxl 开画 + 发起打样

| 天 | 动作 | 产出 |
|---|---|---|
| D1 | 💻 读 `docs/打印就绪检查.md` + `docs/打印工艺调整表.md`；🔨 **打孔径测试片**（调整表第二节，30 分钟） | 测试片 |
| D2 | ✋ 测试片实测：M2 过/攻丝/沉头、轴承 Ø22/Ø15 三档，**把补偿值写进构建日志** | 全机孔径补偿定标 |
| D3 | 💻 照 `docs/imu_to_dxl-设计包/设计规格.md` 在嘉立创 EDA 画原理图（BOM 元件 14 种）；【待核对】项对着 datasheet 填 | 原理图 v1 |
| D4 | 💻 PCB 布局（2 层）；板框/安装孔对照 `cad/` 躯干内腔定尺寸 | PCB v1 |
| D5 | ✋ 检查 PCB v1（引脚网络表对照规格第四节）→ 🛒 **下单打样**（3–5 天货期） | 打样单 |
| D6 | 🔨 开始批量打印：先打**腿部件全套**（试装过的件最稳） | 首批结构件 |
| D7 | 💻 RL 线交汇：跑 exp001 A 组；构建日志补测试片实测数据 | 公众号素材 |

**周退出标准**：孔径补偿定标完成；PCB 已下单；腿部打印件在手。

## 第 2 周（D8–14）：批量打印 + 板子回来 + 单板点亮

| 天 | 动作 | 产出 |
|---|---|---|
| D8–9 | 🔨 打印头壳/躯干壳/脚（耗材大头，约 54 cm³）；按调整表第四节摆件 | 结构件大部分 |
| D10 | ✋ PCB 到货 → 贴片（手焊：0603 + TSSOP20 + SC-70，热风）→ SWD 烧录固件骨架 | 点亮的板 |
| D11 | 💻 补固件两个 TODO：HAL 初始化（I2C/UART）+ LSM6DSV16X 初始化序列（datasheet） | 固件 v1 |
| D12 | ✋ 单板自测（规格第七节 1–2 步）：PING → READ 124 → 晃动看陀螺/四元数变化 | 板子出活数据 |
| D13 | 🔨 继续打印；💻 RL 线：exp001 B 组 + 对比图 | exp001 结论 |
| D14 | 💻 周复盘 + 公众号（硬件线第一篇：测试片与孔径补偿实测） | 文章存稿 |

**周退出标准**：41 件打完 ≥80%；imu_to_dxl 单板出真实 IMU 数据。

## 第 3 周（D15–21）：总线联调 + 躯干装配 + Radxa 起飞

| 天 | 动作 | 产出 |
|---|---|---|
| D15–16 | 🔨 装配躯干底座 + 15 舵机骨架（若舵机已到）：装 IMU 板进躯干，走 EH 线菊花链 | 躯干总成 |
| D17 | ✋ **总线联调**：Radxa `/dev/ttyS2` 一次 sync_read 读回 16 台设备（验收清单第 3 步） | 联调通过 |
| D18 | ✋ mask `serial-getty@ttyS2`、i2c3/FUSB302 引脚取舍、NPU overlay（README 三个必踩的坑逐个过） | 系统底座 |
| D19–20 | 💻 编译部署官方 Rust 运行时（`deploy/` + robotd），装官方 ONNX；🛒 补缺件 | 运行时上机 |
| D21 | 💻 RL 线：发起 GPU 长训练（或 `--hf-jobs`）；公众号硬件线第二篇（总线联调） | 文章存稿 |

**周退出标准**：`robotd` 能读全总线；躯干立得起来。

## 第 4 周（D22–30）：整机装配 + 上电 + 复盘

| 天 | 动作 | 产出 |
|---|---|---|
| D22–24 | 🔨 装配双腿 + 头颈（构建日志逐件记录问题） | 整机 |
| D25 | ✋ 上电姿态检查（不装腿先空跑 HOME 姿态）；电池电压经舵机自报验证 | 安全上电 |
| D26–27 | ✋ 装官方 ONNX 走路策略，手柄遥控首走（摔了照实记录——那是文章 11 的素材） | **鸭子走路** |
| D28 | 💻 对照 rl-series 文章 11 逐项填「仿真 vs 真机」差异表 | sim2real 实测 |
| D29 | 💻 复盘：构建日志终稿 + 反推数据 vs 实测全表（M2/沉头/攻丝/外形 144×141×264） | 验证结论 |
| D30 | 💻 下一周期计划（重训 or 优化走姿 or 轮滑模式）；公众号收官篇 | 系列完整 |

**周退出标准**：整机至少完成「站立 + 遥控行走尝试」；构建日志无空白行。

---

## 风险与退路

| 风险 | 退路 |
|---|---|
| 舵机货期拖过 2 周 | 先做 B/C/D 链（板子、Radxa、固件），装配后置 |
| imu_to_dxl 首板翻车 | BOM 极简 + 协议层已测试，改板成本低；留 v2 打样预算 |
| 打印件装不上 | 测试片已提前定补偿；个别孔钻修是预期内的（构建日志已验证一次） |
| 官方 ONNX 在复刻件上表现差 | 回到 rl-series：用复刻实测差异表调整 DR 后重训（实验方法论已就绪） |
